# Fullstack ToDo App

Prosty projekt listy zadań łączący Reacta z Node.js.

## Jak uruchomić?
1. Wejdź do folderu `backend` i wpisz `node server.js`.
2. Wejdź do folderu `frontend` i wpisz `npm run dev`.